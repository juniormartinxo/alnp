import { UserDataInterface } from './user-data.interface'

describe('Register user on mailing list use case', () => {
  it('should add user with complete data to mailing list', async () => {
    const users: UserDataInterface[] = []
    // const repo: UserRepository = new InMemoryUsersRepository(users)
    // const useCase: RegisterUserOnMailingList = new RegisterUserOnMailingList(repo)
    // const name = 'John Doe'
    // const email = 'john_doe@app.com'
    // const response = await useCase.registerUserOnMailList({ name, email })
    // const user = repo.findUserByEmail(email)

    // expect((await user).name).toBe(name)
  })
})
